#include <bits/stdc++.h> 
long long Pow(int X, int N)
{
    // Write your code here. 
    if(N == 0){
        return 1;
    }
    if(X == 0){
        return 0;
    }
    long long ans = X * Pow(X, N - 1);
    return ans;
}
